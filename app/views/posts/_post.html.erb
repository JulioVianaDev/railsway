<div id="<%= dom_id post %>">
  <p>
    <strong>User:</strong>
    <%= current_user.email %>
  </p>

  <p>
    <strong>Name:</strong>
    <%= post.name %>
  </p>
  <p>
    <strong>image:</strong>
    <%= image_tag(post.image) if post.image.attached? %>
  </p>

</div>
